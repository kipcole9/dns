defmodule ExDns.Storage.ETS do
  # Called at application start so the storage engine
  # can initialize
  def init(config) do
  end

  # Called to save a zone
  def save(zone, args) do
  end

  # Called to get a zone
  def get(name) do
  end

  # Called at application exit
  def exit do
  end
end
